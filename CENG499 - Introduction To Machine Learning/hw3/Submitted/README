you can run draw.dt file with command for drawing svms and make appropriate calculations:
- python3 draw.dt

you can use dt_mini_test.py file with command for running test file:
- python3 dt_mini_test.py

you can draw decision tree and calculate test_accuracy with command:
- python3 decision_tree.py
This command will print the test accuracy to terminal and open the photo of decision tree.
You can also change the mode ('avg_gini_index', 'info_gain') and preprune (0, 1) option with changing variables
in main function in decision_tree.py file.